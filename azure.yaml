# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: snowflake-mcp-server
metadata:
  template: snowflake-mcp-server@0.0.1-beta
services:
  web:
    project: src/
    language: dotnet
    host: appservice